import pygetwindow as gw

class resize(object):

    window_names = {"steam"}
    def resize(arr):
        for i in arr:
            try:
                window = gw.getWindowWithTitle(i)
            except:
                print("window not found!")
                continue    
            window.resizeTo(800,600)
            window.moveTo(100,100)
            



            